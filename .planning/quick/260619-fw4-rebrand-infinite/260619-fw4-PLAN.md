---
phase: quick-260619-fw4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - medelite-report/src/components/SnapshotApp.tsx
  - medelite-report/src/app/layout.tsx
  - medelite-report/README.md
  - CLAUDE.md
autonomous: true
requirements: [REBRAND-01, REBRAND-02, REBRAND-03]
must_haves:
  truths:
    - "The web UI left-pane header shows the INFINITE — Managed by MEDELITE logo image instead of the plain text 'Infinite Snapshot'"
    - "Screen readers still encounter an accessible heading for the web app"
    - "The browser tab title reads 'Infinite' (not 'Infinite Snapshot')"
    - "README and CLAUDE.md narrative product name reads 'Infinite'"
    - "The locked in-report (PDF/preview) header block is unchanged — header.test.ts still passes"
    - "npm run verify is green"
  artifacts:
    - path: "medelite-report/src/components/SnapshotApp.tsx"
      provides: "Web header rendered as the INFINITE logo image with an accessible heading"
      contains: "INFINITE_LOGO_DATA_URI"
    - path: "medelite-report/src/app/layout.tsx"
      provides: "Page metadata title 'Infinite'"
      contains: "title: \"Infinite\""
    - path: "medelite-report/README.md"
      provides: "Rebranded README heading and intro"
    - path: "CLAUDE.md"
      provides: "Rebranded Project-section narrative"
  key_links:
    - from: "medelite-report/src/components/SnapshotApp.tsx"
      to: "medelite-report/src/lib/report/logo.ts"
      via: "import of INFINITE_LOGO_DATA_URI / INFINITE_LOGO_WIDTH / INFINITE_LOGO_HEIGHT"
      pattern: "from \"@/lib/report/logo\""
---

<objective>
Rebrand the web app surface from "Infinite Snapshot" to "Infinite": swap the left-pane plain-text `<h1>` for the INFINITE — Managed by MEDELITE logo image, set the browser tab title to "Infinite", and update the narrative product name in README.md and CLAUDE.md.

Purpose: The product is now named "Infinite". The web header should carry the same static brand logo the report already uses, while keeping the in-report header block (PDF/preview) locked per CLAUDE.md Standing Rule #2.
Output: Updated web header (logo image + accessible heading), page title, README, and CLAUDE.md Project section. The locked in-report header, `assembleHeader()`, and `header.test.ts` remain untouched and green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

<interfaces>
<!-- Logo module exports (medelite-report/src/lib/report/logo.ts) — use these directly, no exploration. -->
INFINITE_LOGO_WIDTH  = 224
INFINITE_LOGO_HEIGHT = 51
INFINITE_LOGO_DATA_URI = "data:image/png;base64,..."  // static INFINITE / Managed-by-MEDELITE brand PNG

<!-- Reference render pattern in medelite-report/src/components/ReportPreview.tsx (lines 47-51 import, 132-138 render): -->
import {
  INFINITE_LOGO_DATA_URI,
  INFINITE_LOGO_WIDTH,
  INFINITE_LOGO_HEIGHT,
} from "@/lib/report/logo";

// inside JSX (note the eslint-disable for the data-URI img):
// eslint-disable-next-line @next/next/no-img-element -- data-URI logo, no Next/Image optimization needed
<img
  src={INFINITE_LOGO_DATA_URI}
  alt={vm.header.platformLine}      // in ReportPreview alt is vm.header.platformLine; in SnapshotApp use the static literal "INFINITE — Managed by MEDELITE"
  width={INFINITE_LOGO_WIDTH}
  height={INFINITE_LOGO_HEIGHT}
  className="mx-auto"
/>
</interfaces>

<current_state>
<!-- medelite-report/src/components/SnapshotApp.tsx lines 152-158 (left pane) currently: -->
// <h1 className="text-2xl font-semibold tracking-tight text-zinc-900">
//   Infinite Snapshot
// </h1>
// <p className="text-sm text-zinc-500">Enter a CMS Certification Number (CCN) to generate a facility assessment snapshot.</p>

<!-- medelite-report/src/app/layout.tsx line 16: title: "Infinite Snapshot"  (description on line 17 stays unchanged) -->

<!-- README.md line 1: "# Infinite Snapshot"; line 3 starts "Infinite Snapshot is a lightweight web app...";
     line 5 deployment URL https://infinite-snapshot.vercel.app MUST stay unchanged. -->

<!-- CLAUDE.md Project section: line 79 "**Infinite Snapshot**"; line 81 "Infinite Snapshot is a lightweight web app...";
     line 83 "...named **Infinite Snapshot** (app/repo/page title)...".
     Do NOT touch Standing Rules, Field Mapping, Commands, or any
     "INFINITE — Managed by MEDELITE" / "FACILITY ASSESSMENT SNAPSHOT" strings. Standing Rule #2 stays verbatim. -->
</current_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap web header to the INFINITE logo image and set page title to "Infinite"</name>
  <files>medelite-report/src/components/SnapshotApp.tsx, medelite-report/src/app/layout.tsx</files>
  <action>
In `medelite-report/src/components/SnapshotApp.tsx`:
- Add an import of `INFINITE_LOGO_DATA_URI`, `INFINITE_LOGO_WIDTH`, `INFINITE_LOGO_HEIGHT` from `@/lib/report/logo` (place it alongside the existing component/lib imports, mirroring ReportPreview.tsx's import group).
- Replace the plain-text left-pane heading (currently `<h1 className="text-2xl font-semibold tracking-tight text-zinc-900">Infinite Snapshot</h1>`, ~lines 152-154) with the INFINITE logo image, mirroring the ReportPreview.tsx render pattern. Keep an accessible heading for screen readers: wrap the `<img>` in an `<h1>` (so the heading semantics survive) and give the img the alt text. Use the static literal string `"INFINITE — Managed by MEDELITE"` as the `alt` (per Standing Rule #2 — the logo label is static, NOT the facility name and NOT a vm value here, since SnapshotApp has no guaranteed vm). Add the eslint-disable line `// eslint-disable-next-line @next/next/no-img-element -- data-URI logo, no Next/Image optimization needed` directly above the `<img>` to keep lint green (ReportPreview.tsx uses this exact comment). You may keep a sensible `className` (e.g. drop `mx-auto` since the left pane is left-aligned, or keep whatever renders cleanly) — left-pane layout is Claude's discretion; do not center it if it looks wrong against the existing left-aligned column.
- Leave the descriptive `<p>` ("Enter a CMS Certification Number (CCN)...") unchanged.
- Do NOT touch anything below the header (search bar, error routing, manual inputs, preview wiring).

In `medelite-report/src/app/layout.tsx`:
- Change line 16 `title: "Infinite Snapshot"` to `title: "Infinite"`. Leave the `description` field (line 17) exactly as-is.

Do NOT change ReportPreview.tsx, ReportPDF.tsx, src/lib/report/header.ts, or logo.ts — the in-report header block is locked (Standing Rule #2) and `assembleHeader()` still takes no facility-name arg. Every .tsx file must keep an import/export (isolatedModules) — the new import preserves that for SnapshotApp.tsx.
  </action>
  <verify>
    <automated>cd medelite-report && npm run verify</automated>
  </verify>
  <done>SnapshotApp's left-pane header renders the INFINITE logo `<img>` (alt="INFINITE — Managed by MEDELITE") inside an accessible `<h1>`; layout.tsx title is "Infinite"; `npm run verify` is green including the unchanged `tests/lib/report/header.test.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Rebrand narrative product name in README.md and CLAUDE.md</name>
  <files>medelite-report/README.md, CLAUDE.md</files>
  <action>
In `medelite-report/README.md`:
- Line 1: `# Infinite Snapshot` → `# Infinite`.
- Line 3: the opening sentence `Infinite Snapshot is a lightweight web app...` → `Infinite is a lightweight web app...`.
- KEEP the deployment URL `https://infinite-snapshot.vercel.app` (line 5) unchanged — the Vercel URL is not affected by the GitHub repo rename.

In `CLAUDE.md` (the root project file), update ONLY the "## Project" section narrative:
- Line 79: `**Infinite Snapshot**` → `**Infinite**`.
- Line 81: `Infinite Snapshot is a lightweight web app...` → `Infinite is a lightweight web app...`.
- Line 83: in the sentence `...The product is named **Infinite Snapshot** (app/repo/page title)...`, change `**Infinite Snapshot**` → `**Infinite**`. Adjust the `(app/repo/page title)` wording only if needed for accuracy; minimal edit preferred.
- Do NOT touch Standing Rules (especially Standing Rule #2 — verbatim), Field Mapping, the Next.js caveat, Commands, or ANY occurrence of `INFINITE — Managed by MEDELITE` / `FACILITY ASSESSMENT SNAPSHOT` / `INFINITE` brand strings. Those are static branding and must remain exactly as written.
  </action>
  <verify>
    <automated>cd /Users/akashkalita/Infinite-Snapshot && ! grep -n "Infinite Snapshot" CLAUDE.md medelite-report/README.md && grep -c "INFINITE — Managed by MEDELITE" CLAUDE.md</automated>
  </verify>
  <done>No remaining "Infinite Snapshot" string in CLAUDE.md or README.md; README heading is "# Infinite"; the Vercel URL infinite-snapshot.vercel.app is preserved; all "INFINITE — Managed by MEDELITE" / "FACILITY ASSESSMENT SNAPSHOT" branding strings and Standing Rule #2 are untouched.</done>
</task>

</tasks>

<verification>
- `cd medelite-report && npm run verify` is green (typecheck → lint → format:check → test), including the unchanged `tests/lib/report/header.test.ts`.
- The web tab title is "Infinite"; the left-pane header is the INFINITE logo image with an accessible `<h1>` and alt="INFINITE — Managed by MEDELITE".
- `grep "Infinite Snapshot" CLAUDE.md medelite-report/README.md` returns nothing.
- `grep "INFINITE — Managed by MEDELITE" CLAUDE.md` still matches (branding preserved); Standing Rule #2 text unchanged.
- In-report header (ReportPreview.tsx / ReportPDF.tsx / header.ts) and `assembleHeader()` signature are untouched.
</verification>

<success_criteria>
- Web header renders the static INFINITE — Managed by MEDELITE logo image (logo-image-only) with a preserved accessible heading.
- Browser tab / metadata title reads "Infinite".
- README.md and CLAUDE.md Project narrative read "Infinite"; deployment URL unchanged.
- Locked in-report header block, header.ts, and header.test.ts are unchanged and green.
- `npm run verify` passes; commits are atomic (one per task).
</success_criteria>

<orchestrator_followup>
NOT an executor task (outward-facing, not subject to `npm run verify`): the orchestrator handles the GitHub repo rename after execution —
- Rename the repo slug to `Infinite-MedElite`.
- Set the GitHub About/description to "Infinite - Managed by MedElite".
- The README deployment URL stays `infinite-snapshot.vercel.app` (Vercel URL is independent of the GitHub repo slug).
Do NOT place `gh repo rename` or `git remote set-url` in any executor task.
</orchestrator_followup>

<output>
Create `.planning/quick/260619-fw4-rebrand-infinite/260619-fw4-SUMMARY.md` when done
</output>
