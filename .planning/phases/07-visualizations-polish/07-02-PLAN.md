---
phase: 07-visualizations-polish
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - medelite-report/package.json
  - medelite-report/package-lock.json
  - medelite-report/next.config.ts
  - medelite-report/src/components/MiniBarChart.tsx
  - medelite-report/src/components/pdf/PdfMiniBarChart.tsx
  - medelite-report/src/components/pdf/PdfMiniBarChart.test.ts
  - medelite-report/src/lib/charts/rasterize.ts
  - medelite-report/src/lib/charts/chart-svg.ts
  - medelite-report/src/components/ReportPreview.tsx
  - medelite-report/src/components/pdf/ReportPDF.tsx
  - medelite-report/src/lib/docx/ReportDocx.ts
  - medelite-report/src/lib/charts/chart-svg.test.ts
  - medelite-report/tests/lib/charts/rasterize.test.ts
autonomous: false
requirements: [VIZ-01, VIZ-02]
user_setup: []

must_haves:
  truths:
    - "Below the 12 verbatim metric rows, the web preview shows 4 mini grouped-bar charts (one per measure), each with facility/national/state bars in blue/green/amber WITH a legend (D-07/D-08)"
    - "The downloaded PDF shows the same 4 charts via react-pdf-charts (recharts wrapped, isAnimationActive=false) WITH a legend — filled bars, not blank rectangles (VIZ-02, D-08)"
    - "The downloaded .docx shows the same 4 charts as embedded PNG images rasterized server-side from the chart SVG (D-11)"
    - "A suppressed value omits that bar and shows an 'N/A' indication; a partially-suppressed measure still renders its chart with the available bars (D-09)"
    - "The .docx with 4 chart PNGs stays under 4,500,000 bytes (DOCX-01)"
    - "The 12 verbatim CLM-03 rows still render unchanged above the charts; charts are ADDED below, never replacing rows (D-03)"
  artifacts:
    - path: "medelite-report/src/lib/charts/chart-svg.ts"
      provides: "Pure buildChartData(group) → {name,value,color}[] with nulls filtered (D-09) + renderChartSvgString(data) via renderToStaticMarkup (server-only, isAnimationActive=false)"
      exports: ["buildChartData", "renderChartSvgString"]
    - path: "medelite-report/src/lib/charts/rasterize.ts"
      provides: "svgToPngBuffer(svg,w,h) → PNG Buffer via @resvg/resvg-js (server-only)"
      exports: ["svgToPngBuffer"]
    - path: "medelite-report/src/components/MiniBarChart.tsx"
      provides: "Web recharts v2 grouped-bar chart for one measure group, with legend + N/A empty state"
      exports: ["MiniBarChart"]
    - path: "medelite-report/src/components/pdf/PdfMiniBarChart.tsx"
      provides: "PDF react-pdf-charts bar chart (isAnimationActive=false, with Legend per D-08) for one measure group"
      exports: ["PdfMiniBarChart"]
    - path: "medelite-report/src/components/pdf/PdfMiniBarChart.test.ts"
      provides: "Structural unit test: asserts the PdfMiniBarChart element tree wraps content in ReactPDFChart, every <Bar> carries isAnimationActive={false}, and a <Legend> is present; empty group → no <Bar>"
  key_links:
    - from: "medelite-report/src/components/ReportPreview.tsx"
      to: "MiniBarChart + groupByMeasure"
      via: "section below </table> mapping groupByMeasure(vm.hospMetrics)"
      pattern: "MiniBarChart"
    - from: "medelite-report/src/components/pdf/ReportPDF.tsx"
      to: "PdfMiniBarChart + groupByMeasure"
      via: "View after the metric rows mapping groupByMeasure(vm.hospMetrics)"
      pattern: "PdfMiniBarChart"
    - from: "medelite-report/src/lib/docx/ReportDocx.ts"
      to: "svgToPngBuffer + renderChartSvgString"
      via: "4 ImageRun PNGs injected into the docx (callback-form replace)"
      pattern: "svgToPngBuffer"
    - from: "medelite-report/next.config.ts"
      to: "serverExternalPackages"
      via: "@resvg/resvg-js added (NAPI binary, not auto-external)"
      pattern: "@resvg/resvg-js"
---

<objective>
Deliver the claims-metric charts end-to-end across all three renderers, building on Plan 01's D-15 grouping foundation and shared color constants.

After this plan: below the 12 verbatim metric rows, a user sees 4 mini grouped-bar charts (one per measure, facility/national/state bars in blue/green/amber with a legend) in the web preview, the downloaded PDF (react-pdf-charts), and the downloaded .docx (embedded PNGs) — with suppressed bars omitted and an "N/A" indication, partial charts still rendering. The .docx stays under 4.5 MB. The 12 CLM-03 rows are untouched; charts are added below them (D-03).

Purpose: Closes the chart half of VIZ-01 (web) and VIZ-02 (PDF), plus the .docx full-visuals goal (D-11). This is the second vertical slice — one cohesive charting capability across all renderers — not a horizontal "all web then all PDF" cut.

Output: @resvg/resvg-js install + next.config update, MiniBarChart + PdfMiniBarChart components, chart-svg + rasterize server-only lib, and chart injection into ReportPreview, ReportPDF, and ReportDocx.

NOTE: This plan is `autonomous: false` — Task 1 contains a blocking `checkpoint:human-verify` for the `[ASSUMED]` `@resvg/resvg-js` package legitimacy (slopcheck was unavailable at research time).

NOTE (file load discipline): this plan touches 12 files because each chart is a three-renderer (web + PDF + docx) vertical slice; the executor should `<read_first>`/load files on demand per task rather than loading all 12 up front.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@medelite-report/AGENTS.md
@.planning/phases/07-visualizations-polish/07-CONTEXT.md
@.planning/phases/07-visualizations-polish/07-PATTERNS.md
@.planning/phases/07-visualizations-polish/07-RESEARCH.md
@.planning/phases/07-visualizations-polish/07-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 (already shipped) + the codebase. Use directly. -->

From src/lib/report/chart-utils.ts (Plan 01):
```typescript
type MeasureKey = "521" | "522" | "551" | "552";
type MeasureGroup = { facility?: HospMetric; nation?: HospMetric; state?: HospMetric; label: string; unit: "percent" | "rate" };
export function groupByMeasure(metrics: HospMetric[]): MeasureGroup[]; // 4 groups, fixed order
```

From src/lib/report/colors.ts (Plan 01):
```typescript
export const CHART_SERIES = { facility: "#3b82f6", nation: "#16a34a", state: "#f59e0b" } as const; // series identity, NOT performance bands (D-08)
```

From src/lib/cms/types.ts (Plan 01): HospMetric now has `measureKey` + `source`; `value: number | null`; `unit: "percent" | "rate"`.

From src/components/ReportPreview.tsx — charts go AFTER `</table>` (line 208), before `<footer>` (line 214), inside the `<article>`. The `vm.hospMetrics === undefined` degraded guard (lines 189–207) stays.

From src/components/pdf/ReportPDF.tsx — charts go after the hospMetrics map block, inside the `styles.table` View (after line 268, before `</View>` line 269). react-pdf has NO keyed Fragment — key the wrapping View.

From src/lib/docx/ReportDocx.ts — buildReportDocxBuffer (JSZip). Inject ImageRun/`<w:drawing>` PNGs after the main fill, using callback-form replace (CR-01); the < 4_500_000 assertion in tests/api/export-docx.test.ts line 135 must stay green.

From react-pdf-charts (installed): `import ReactPDFChart from "react-pdf-charts"` — wraps recharts; calls renderToStaticMarkup. `isAnimationActive={false}` MANDATORY on every <Bar> (Pitfall 1). recharts `<Legend>` passes through renderToStaticMarkup into react-pdf SVG, so a PDF chart can carry a real legend (D-08).

TEST MECHANISM (node env, no jsdom): the existing Vitest config is `environment: "node"` with `include: ["tests/**/*.test.ts", "src/**/*.test.ts"]` — `.test.tsx` is NOT included and will never run. To structurally test a PDF chart component WITHOUT a DOM, import the component function from the `.tsx` file inside a `.test.ts` file (esbuild transpiles the imported JSX), call it as a plain function to get its returned React element, and walk the `type`/`props.children` tree (a small recursive `findByType(el, predicate)` helper) to assert structure. No `renderToBuffer`/DOM is required for the structural assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install @resvg/resvg-js (legitimacy gate) + chart-svg/rasterize server-only lib</name>
  <read_first>
    - 07-RESEARCH.md "Package Legitimacy Audit" (@resvg/resvg-js [ASSUMED]), "New Package Required" + Pattern 5 (rasterize), Pattern 4 mechanism, Pitfall 2 (serverExternalPackages), Open Question 1 (recharts renderToStaticMarkup server feasibility)
    - 07-PATTERNS.md sections "src/lib/charts/rasterize.ts" + "next.config.ts (MODIFY)" + "isAnimationActive={false} — Mandatory" + "Server-Only Module Discipline"
    - medelite-report/next.config.ts (current serverExternalPackages line 12)
    - medelite-report/src/lib/report/colors.ts + chart-utils.ts (Plan 01)
  </read_first>
  <behavior>
    - buildChartData(group) for a full group → 3 entries [{name:"Facility",value,color:CHART_SERIES.facility}, {name:"National",...nation}, {name:"State",...state}] with `=== null` values filtered OUT (D-09).
    - buildChartData(group) where only facility is present → 1 entry; where all suppressed → [] (caller renders an N/A indication).
    - renderChartSvgString([{name,value,color}…]) returns a non-empty SVG string containing `<svg` and `<rect`/`<path` bar geometry (proves recharts → renderToStaticMarkup works server-side, isAnimationActive=false — Open Question 1).
    - svgToPngBuffer(svg) returns a Buffer whose first 8 bytes are the PNG magic (0x89 0x50 0x4E 0x47 0x0D 0x0A 0x1A 0x0A).
  </behavior>
  <action>
    FIRST: a blocking package-legitimacy checkpoint for `@resvg/resvg-js` (see the checkpoint task below — it precedes the install). Do NOT run `npm install` until the checkpoint is approved.

    Install from `medelite-report/`: `npm install @resvg/resvg-js@^2.6.2`. Confirm the optional NAPI dep `@resvg/resvg-js-darwin-*` (local) installed and that `@resvg/resvg-js-linux-x64-gnu` is listed in optionalDependencies (Vercel Lambda target).

    Update `next.config.ts`: change `serverExternalPackages: ["@react-pdf/renderer"]` to `["@react-pdf/renderer", "@resvg/resvg-js"]` and extend the rationale comment to mention the @resvg NAPI `.node` binary is not in Next.js auto-external list (Pitfall 2).

    Create `src/lib/charts/chart-svg.ts` (server-only, NO "use client"):
      - `buildChartData(group: MeasureGroup)`: map facility/nation/state slots to `{name, value, color}` using `CHART_SERIES`, filter `value === null` (D-09). Export the `ChartDatum` type.
      - `renderChartSvgString(data: ChartDatum[], width=300, height=120): string`: use `renderToStaticMarkup` from `react-dom/server` + `createElement` to build a recharts `<BarChart width height data>` with `<XAxis dataKey="name">`, `<YAxis>`, and `<Bar dataKey="value" isAnimationActive={false}>` containing `<Cell fill={d.color}>` per datum. `isAnimationActive={false}` is mandatory (Pitfall 1). This shared SVG generator is reused by the docx rasterizer; the web/PDF use their own component wrappers but the same CHART_SERIES + data shape.

    Create `src/lib/charts/rasterize.ts` (server-only, NO "use client"): `import { Resvg } from "@resvg/resvg-js"`; `svgToPngBuffer(svgString, width=300, height=120): Buffer` → `new Resvg(svgString, { fitTo: { mode: "width", value: width } }).render()` → `Buffer.from(pngData.asPng())`. Add the server-only discipline comment (must never reach a client bundle).

    Tests (both `.test.ts`, node env): `src/lib/charts/chart-svg.test.ts` covers buildChartData null-filtering + the SVG-string non-empty/`<svg` assertion (this is the Open Question 1 smoke test — if renderToStaticMarkup throws on recharts, fail here, not in production). `tests/lib/charts/rasterize.test.ts` covers svgToPngBuffer PNG-magic-bytes on a small hand-written SVG.
  </action>
  <verify>
    <automated>cd medelite-report && npx vitest run src/lib/charts/chart-svg.test.ts tests/lib/charts/rasterize.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@resvg/resvg-js" medelite-report/package.json` shows `^2.6.2` in dependencies; `grep -n "@resvg/resvg-js" medelite-report/next.config.ts` shows it in serverExternalPackages.
    - `chart-svg.test.ts` passes: buildChartData filters nulls (D-09) AND renderChartSvgString returns a string containing `<svg` (proves recharts renderToStaticMarkup works server-side — Open Question 1 resolved).
    - `rasterize.test.ts` passes: svgToPngBuffer output begins with PNG magic bytes 0x89 0x50 0x4E 0x47.
    - `grep -c "isAnimationActive" medelite-report/src/lib/charts/chart-svg.ts` ≥ 1 (mandatory for clean SVG output).
    - `grep -L "use client" medelite-report/src/lib/charts/rasterize.ts medelite-report/src/lib/charts/chart-svg.ts` lists BOTH files (server-only — no "use client").
  </acceptance_criteria>
  <done>@resvg/resvg-js installed behind the legitimacy gate + in serverExternalPackages; chart-svg + rasterize server-only modules exist with passing tests proving recharts→SVG→PNG works server-side.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1a: Package legitimacy checkpoint — @resvg/resvg-js [ASSUMED]</name>
  <action>Pause execution and present the verification steps below to the user; do NOT run `npm install @resvg/resvg-js` until the user replies "approved". This is a blocking, non-auto-approvable legitimacy gate.</action>
  <what-built>Nothing yet — this checkpoint precedes the `npm install @resvg/resvg-js@^2.6.2` in Task 1. RESEARCH.md tagged this package `[ASSUMED]` because slopcheck was unavailable at research time. Per the package-legitimacy gate, an [ASSUMED] package must be human-verified before install. This checkpoint is NOT auto-approvable (workflow.auto_advance is ignored for legitimacy gates).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@resvg/resvg-js and confirm: package exists, version 2.6.2 is published, source repo is github.com/yisibl/resvg-js, weekly downloads are non-trivial, and `@resvg/resvg-js-linux-x64-gnu` appears under optionalDependencies (the Vercel Lambda binary).
    2. Confirm the repo is the real resvg-js project (NAPI-RS, pure-Rust SVG renderer) and not a typosquat.
    3. Optionally check the resvg-js Vercel playground (resvg-js.vercel.app) confirming Vercel-runtime support.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or "use wasm" to fall back to @resvg/resvg-wasm (no NAPI binary), or describe a blocking concern.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MiniBarChart (web) + PdfMiniBarChart (PDF) and their wiring below the 12 rows</name>
  <read_first>
    - medelite-report/src/components/ReportPreview.tsx ("use client" line 1; the `</table>` line 208 and `<footer>` line 214 — charts go between; the degraded guard 189–207)
    - medelite-report/src/components/pdf/ReportPDF.tsx (the hospMetrics map block 252–268; styles.table View; import block)
    - medelite-report/src/lib/report/chart-utils.ts (groupByMeasure) + colors.ts (CHART_SERIES) + charts/chart-svg.ts (buildChartData) from Plan 01/Task 1
    - 07-RESEARCH.md Pattern 3 (web recharts), Pattern 4 (PDF react-pdf-charts), Pitfall 1 (charts blank in PDF), Pitfall 5 (recharts+react-pdf bundle scope), D-08 legend note, D-09 suppression
    - 07-PATTERNS.md sections "src/components/MiniBarChart.tsx", "src/components/pdf/PdfMiniBarChart.tsx", "src/components/ReportPreview.tsx (MODIFY)", "src/components/pdf/ReportPDF.tsx (MODIFY)"
    - the TEST MECHANISM note in <context> (import the .tsx component into a .test.ts, walk the returned element tree — node env, no jsdom)
  </read_first>
  <behavior>
    - MiniBarChart({group}) for a full group renders a recharts BarChart with 3 colored bars (facility blue / national green / state amber) + a legend mapping name→color (D-08); axis ticks small; tooltip formats percent vs rate by group.unit.
    - MiniBarChart for an all-suppressed group renders a small "N/A" indication, NOT an empty chart frame (D-09).
    - MiniBarChart for a partial group renders only the present bars (D-09 partial-chart).
    - PdfMiniBarChart({group}) wraps recharts in <ReactPDFChart> with `isAnimationActive={false}` on the <Bar> AND includes a recharts `<Legend>` (D-08 — the PDF chart carries the same facility/national/state legend as the web chart; react-pdf-charts passes <Legend> through renderToStaticMarkup into react-pdf SVG); returns null (or an N/A Text) when no bars (D-09).
    - STRUCTURAL UNIT (Blocker 2 — TDD, written alongside the component per CLAUDE.md rule #5): `PdfMiniBarChart` for a non-empty group returns an element tree whose chart content is wrapped in `ReactPDFChart`, every `<Bar>` node carries `isAnimationActive === false`, and at least one `<Legend>` node is present (this single test simultaneously guards Pitfall 1 and Blocker 1/D-08). For an all-suppressed group it returns the N/A Text / null path with NO `<Bar>` node. The integration `export-pdf.test.ts` only proves `renderToBuffer` doesn't crash; this structural test is what actually catches a missing legend / missing `isAnimationActive` / a chart not wrapped in the adapter.
    - After wiring: ReportPreview shows 4 charts in a grid below the table; ReportPDF shows 4 charts after the 12 metric rows; the 12 verbatim rows are unchanged in both (D-03); the `hospMetrics === undefined` degraded path renders no chart section.
  </behavior>
  <action>
    Create `src/components/MiniBarChart.tsx` ("use client" — recharts is browser-only). Props `{ group: MeasureGroup }`. Build data via `buildChartData(group)` (Task 1) — if empty, return a small `<span className="text-xs text-zinc-400">N/A</span>` (D-09). Otherwise render `<ResponsiveContainer width="100%" height={140}><BarChart data={data} …>` with `<XAxis dataKey="name" tick small>`, `<YAxis>`, `<Tooltip formatter>` (percent→1dp+"%", rate→2dp via group.unit), `<Legend>`, and `<Bar dataKey="value">{data.map(d => <Cell fill={d.color}/>)}</Bar>`. Add a small `<h3>` caption with `group.label`. Colors come from CHART_SERIES via buildChartData — keep visually distinct from the star bands (D-08).

    Create `src/components/pdf/PdfMiniBarChart.tsx` (NO "use client" — server-only). Props `{ group: MeasureGroup }`. `import ReactPDFChart from "react-pdf-charts"`, `{ BarChart, Bar, XAxis, YAxis, Cell, Legend } from "recharts"`, `{ View, Text } from "@react-pdf/renderer"`. Build data via `buildChartData(group)`; empty → `<Text>N/A</Text>` or null (D-09). Otherwise `<View><ReactPDFChart><BarChart width={300} height={140} data={data}><XAxis dataKey="name" tick small/><YAxis tick small/><Legend/><Bar dataKey="value" isAnimationActive={false}>{data.map(d => <Cell fill={d.color}/>)}</Bar></BarChart></ReactPDFChart></View>`. The `<Legend>` is MANDATORY (D-08 — Blocker 1): react-pdf-charts renders the recharts `<Legend>` through `renderToStaticMarkup` into react-pdf SVG, so the PDF chart shows the same facility/national/state legend as the web chart; bump the chart height to ~140 to leave room for the legend row. `isAnimationActive={false}` is MANDATORY (Pitfall 1). If a mismatched-reconciler error appears at build, add `react-pdf-charts` to serverExternalPackages (Pitfall 5) — note this in the SUMMARY if done.

    Create `src/components/pdf/PdfMiniBarChart.test.ts` (`.test.ts`, node env, NO jsdom — Blocker 2): import `PdfMiniBarChart` from the `.tsx` file and the `MeasureGroup`/`HospMetric` types; build a full-group fixture (facility+nation+state HospMetrics) and an all-suppressed fixture. Call `PdfMiniBarChart({ group })` directly to get the returned React element. Write a small recursive `findByType(node, predicate): node[]` helper that walks `props.children` (handling arrays + nested elements) and matches on the element's `type` (compare against the imported `ReactPDFChart`, `Bar`, and `Legend` references — react-pdf-charts/recharts elements expose their component function/displayName as `type`). Assert: (1) for the full group, ≥1 `ReactPDFChart` node, ≥1 `Bar` node, every `Bar` node has `props.isAnimationActive === false`, and ≥1 `Legend` node; (2) for the all-suppressed group, zero `Bar` nodes (N/A / null path). This is element-tree inspection of the returned element — no `renderToBuffer`/DOM needed, so it runs in node env.

    Wire ReportPreview: import `MiniBarChart` + `groupByMeasure`. After `</table>` (line 208) and before `<footer>` (line 214), add a `<section>` that renders ONLY when `vm.hospMetrics && vm.hospMetrics.length > 0` (skip on the degraded `undefined` path), with a heading and a `grid grid-cols-2 gap-4` mapping `groupByMeasure(vm.hospMetrics)` to `<MiniBarChart>`. The 12-row `<table>` body and renderMetricValue stay UNCHANGED (D-03/CLM-03).

    Wire ReportPDF: import `PdfMiniBarChart` + `groupByMeasure`. After the hospMetrics map block (after line 268, before the `styles.table` View closes at 269), add a `<View style={{ marginTop: 8 }}>` rendering when `vm.hospMetrics && vm.hospMetrics.length > 0`, mapping `groupByMeasure(vm.hospMetrics)` to `<PdfMiniBarChart key={i} group={group} />` (key the View — react-pdf has no keyed Fragment). The 12 PdfRow metric rows stay UNCHANGED.
  </action>
  <verify>
    <automated>cd medelite-report && npx vitest run src/components/pdf/PdfMiniBarChart.test.ts tests/api/export-pdf.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MiniBarChart" medelite-report/src/components/ReportPreview.tsx` and `grep -n "PdfMiniBarChart" medelite-report/src/components/pdf/ReportPDF.tsx` both show the chart section wired below the metric rows.
    - `grep -c "isAnimationActive={false}" medelite-report/src/components/pdf/PdfMiniBarChart.tsx` ≥ 1 (Pitfall 1 — blank-chart guard).
    - `grep -c "ReactPDFChart" medelite-report/src/components/pdf/PdfMiniBarChart.tsx` ≥ 1 (charts go through the adapter — VIZ-02, never raw recharts in <Document>).
    - `grep -c "Legend" medelite-report/src/components/pdf/PdfMiniBarChart.tsx` ≥ 1 (D-08 / Blocker 1 — the PDF chart carries a legend, matching the web chart).
    - `PdfMiniBarChart.test.ts` passes (Blocker 2): the structural test asserts the element tree wraps content in ReactPDFChart, every <Bar> has `isAnimationActive === false`, and a <Legend> is present; the all-suppressed group has no <Bar>.
    - `tests/api/export-pdf.test.ts` passes: PDF renders without throwing (renderToBuffer of the full tree incl. PdfMiniBarChart succeeds), magic bytes present, AND the existing 12-row CLM-03 assertions still hold (rows + charts coexist, D-03).
    - `npx tsc --noEmit` clean.
    - Manual-only (Plan 03 SC#4): open the downloaded PDF in a real reader — confirm 4 charts with colored bars + a legend (NOT blank rectangles); web preview shows 4 charts + legend.
  </acceptance_criteria>
  <done>MiniBarChart + PdfMiniBarChart exist (PDF chart carries a Legend per D-08); the PdfMiniBarChart structural .test.ts proves ReactPDFChart wrapping + isAnimationActive=false + Legend present; 4 charts render below the 12 rows in web preview + PDF; suppressed/partial handled (D-09); 12 CLM-03 rows unchanged; export-pdf + tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: docx embedded chart PNGs (server-side rasterization) under 4.5 MB</name>
  <read_first>
    - medelite-report/src/lib/docx/ReportDocx.ts (buildReportDocxBuffer JSZip flow; the footer/sectPr callback-replace 188–195; the rels injection 197–218; the star runs from Plan 01)
    - medelite-report/src/lib/charts/chart-svg.ts (renderChartSvgString) + rasterize.ts (svgToPngBuffer) + chart-utils.ts (groupByMeasure) from Plan 01/Task 1
    - medelite-report/tests/api/export-docx.test.ts (the < 4_500_000 size guard line 135–139; PK-ZIP magic; template-fill assertions)
    - 07-RESEARCH.md Pattern 5 (SVG→PNG + how to inject ImageRun), Pitfall 3 (docx size with PNGs — keep 300×120), Pitfall 2 (resvg on Vercel)
    - 07-PATTERNS.md section "src/lib/docx/ReportDocx.ts (MODIFY)" item 4 (ImageRun chart PNGs) + the CR-01 callback discipline
  </read_first>
  <behavior>
    - When `vm.hospMetrics` is present, buildReportDocxBuffer embeds 4 chart PNGs (one per measure group) into the docx zip (word/media/chart-N.png) referenced by valid `<w:drawing>`/relationship markup, rendered after the table body.
    - When `vm.hospMetrics` is undefined (D-09 degraded), NO chart PNGs are added.
    - A suppressed/partial group's chart PNG renders only the available bars (driven by buildChartData filtering); an all-suppressed group renders no bar but does not crash.
    - buildReportDocxBuffer still returns valid PK-ZIP OOXML bytes < 4,500,000 WITH the 4 PNGs included (DOCX-01, Pitfall 3).
  </behavior>
  <action>
    In `buildReportDocxBuffer`, after the existing fill + star injection (Plan 01) and before/around the footer injection, add a chart-embedding pass guarded by `if (vm.hospMetrics && vm.hospMetrics.length > 0)`:
      - `const groups = groupByMeasure(vm.hospMetrics)` (import from `@/lib/report/chart-utils`).
      - For each group: `const svg = renderChartSvgString(buildChartData(group), 300, 140)` (import from `@/lib/charts/chart-svg`), then `const png = svgToPngBuffer(svg, 300, 140)` (import from `@/lib/charts/rasterize`). Keep dimensions ~300×140 to respect the 4.5 MB budget (Pitfall 3). Skip groups whose buildChartData is empty.
      - Add each PNG to the zip via `zip.file("word/media/chart-${i}.png", png)` and add an Image relationship to `word/_rels/document.xml.rels` (mirror the existing rIdCmsLink rels-injection pattern at lines 199–218, but TargetMode internal for media). Use unique relationship IDs (e.g. `rIdChart0…3`) with the same "assert not pre-existing" guard discipline.
      - Inject a paragraph per chart containing a `<w:drawing>` inline image referencing the relationship + the PNG extent (use the px-based transformation lesson from STATE.md DOCX-EMU-01: docx ImageRun transformation takes PIXELS — if using the docx ImageRun API; if injecting raw OOXML `<w:drawing>`, EMU = px × 9525). Inject before the body `<w:sectPr>` (after the existing footer paragraph) using CALLBACK-FORM replace (CR-01) so any `$` is literal.
    All `.replace()` calls that insert chart XML MUST use the `() => fragment` callback form (CR-01). The chart SVG/PNG is derived purely from validated `vm.hospMetrics` numbers — no user free-text reaches the SVG (threat T-7-04).
  </action>
  <verify>
    <automated>cd medelite-report && npx vitest run tests/api/export-docx.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "svgToPngBuffer" medelite-report/src/lib/docx/ReportDocx.ts` and `grep -n "groupByMeasure" medelite-report/src/lib/docx/ReportDocx.ts` show the chart-embedding pass.
    - Extend `tests/api/export-docx.test.ts`: for `validVmWithMetrics`, assert the zip contains ≥1 `word/media/chart-*.png` entry (PNG magic bytes) and ≥1 chart relationship; for a degraded vm (hospMetrics undefined) assert NO chart media entries.
    - The existing `buffer is under 4_500_000 bytes` test still passes WITH the 4 PNGs included (DOCX-01, Pitfall 3) — assert against `validVmWithMetrics`.
    - The existing PK-ZIP magic-bytes + template-fill label-parity + star-run (Plan 01) tests still pass.
    - `grep -c "() =>" medelite-report/src/lib/docx/ReportDocx.ts` confirms chart injection uses callback-form replace (CR-01).
    - Manual-only (Plan 03 SC#4): open the .docx in LibreOffice — confirm 4 chart images appear and file < 4 MB.
  </acceptance_criteria>
  <done>docx embeds 4 chart PNGs rasterized server-side via @resvg/resvg-js; stays < 4_500_000 with images; degraded path adds none; PK-ZIP + star runs + 12 rows intact; export-docx test extended and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/export/pdf and /api/export/docx | Posted ReportViewModel re-validated by ReportViewModelSchema before any chart render/rasterize |
| chart data → SVG string → @resvg/resvg-js → PNG | Chart SVG is built server-side from validated numeric vm.hospMetrics; rasterizer consumes only app-generated SVG |
| new dependency → server bundle | @resvg/resvg-js NAPI binary loaded server-side via serverExternalPackages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-04 | Tampering | SVG fed to @resvg/resvg-js | mitigate | The SVG is generated by `renderChartSvgString` from `buildChartData(group)` — values come from `vm.hospMetrics` (validated `z.number().nullable()` via HospMetricSchema); colors from the closed CHART_SERIES enum. No untrusted/remote SVG, no user-controlled string → no SVG/XXE/SSRF injection surface. |
| T-7-05 | Denial of Service | docx PNG payload size | mitigate | Chart dimensions fixed at ~300×140; only 4 charts; the DOCX-01 `< 4_500_000` test asserts the bound WITH images (Pitfall 3). |
| T-7-06 | Tampering | @resvg/resvg-js supply chain | mitigate | Blocking package-legitimacy checkpoint (Task 1a) before install — verify on npmjs.com/crates source; [ASSUMED] not auto-approvable. Pinned `^2.6.2`. |
| T-7-07 | Tampering | docx chart OOXML/rels injection | mitigate | Chart `<w:drawing>` + relationship XML inserted via CALLBACK-FORM `.replace()` (CR-01) so `$` metachars cannot corrupt OOXML; relationship IDs guarded against pre-existence (mirrors rIdCmsLink guard). |
| T-7-SC | Tampering | npm install (@resvg/resvg-js) | mitigate | Covered by T-7-06 + Task 1a blocking checkpoint; matches the `T-{phase}-SC` slopcheck/human-gate requirement. |
</threat_model>

<verification>
- `cd medelite-report && npm run verify:full` green after Task 3 — this plan adds a NAPI dependency + serverExternalPackages, so the `next build` step (verify:full) is mandatory to catch Turbopack/native-binary bundling failures (Pitfall 2).
- Pure chart logic (buildChartData null-filter, SVG-string generation, PNG magic) covered by chart-svg.test.ts + rasterize.test.ts.
- PDF chart STRUCTURE (ReactPDFChart wrapper, isAnimationActive=false, Legend present) covered by PdfMiniBarChart.test.ts (element-tree inspection, node env); end-to-end render exercised by tests/api/export-pdf.test.ts; docx chart PNGs asserted in tests/api/export-docx.test.ts.
- Regression guards: CLM-03 12-row coexistence (export-pdf/export-docx), DOCX-01 size WITH images, RPT-02 single view-model drives all three.
- Manual-only (Plan 03 SC#4 live smoke): opened PDF charts non-blank + legend visible; opened .docx chart images present + < 4 MB.
</verification>

<success_criteria>
- 4 mini grouped-bar charts render below the 12 rows in web preview, PDF, and .docx (D-03/D-07/D-08).
- Bars are facility/national/state in blue/green/amber WITH a legend in BOTH the web AND PDF chart (D-08); palette distinct from star bands.
- Suppressed bar omitted + N/A indication; partial charts still render (D-09).
- PDF charts via react-pdf-charts with isAnimationActive=false (VIZ-02 — not blank) and a Legend (D-08).
- .docx embeds chart PNGs via @resvg/resvg-js and stays < 4,500,000 bytes (D-11/DOCX-01).
- 12 verbatim CLM-03 rows unchanged.
- `npm run verify:full` green.
</success_criteria>

<output>
Create `.planning/phases/07-visualizations-polish/07-02-SUMMARY.md` when done.
</output>
</output>
